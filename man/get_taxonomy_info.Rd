% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_taxonomy_info.R
\name{get_taxonomy_info}
\alias{get_taxonomy_info}
\title{Get Taxonomy Information Including Species, Year, and Author}
\usage{
get_taxonomy_info(
  species,
  all_categories = FALSE,
  sources = c("gbif", "itis", "ncbi")
)
}
\arguments{
\item{species}{Vector of species names (scientific names) as input.}

\item{all_categories}{Logical, if TRUE, retrieves all taxonomic categories available. Default is FALSE.}

\item{sources}{Character vector, the taxonomy sources to query. Options include "gbif", "itis", "ncbi",
"col", "eol", "tropicos", "wikidata", "ott", "nbnc", "fishbase". Default is \code{c("gbif", "itis", "ncbi")}.}
}
\value{
A data frame with taxonomic categories including species, year, author, genus, family, and order.
}
\description{
This function returns the genus, family, order, and additional information (species, year, author)
for each species in the input vector. It queries multiple sources (e.g., GBIF, ITIS, FishBase, COL, NCBI)
to retrieve the taxonomic information.
If \code{all_categories} is set to TRUE, it will return all available taxonomic ranks.
}
\examples{
\dontrun{
# Example with a single species using multiple sources
get_taxonomy_info(c("Panthera leo"), sources = c("gbif", "itis"))

# Example with fish species using FishBase
get_taxonomy_info(c("Salmo salar", "Oncorhynchus mykiss"), sources = "fishbase")

# Example with multiple species and all categories
get_taxonomy_info(c("Panthera leo", "Homo sapiens"), all_categories = TRUE)
}
}
