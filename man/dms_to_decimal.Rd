% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dms_to_decimal.R
\name{dms_to_decimal}
\alias{dms_to_decimal}
\title{Convert Degrees, Minutes, Seconds (DMS) to Decimal Degrees (DD)}
\usage{
dms_to_decimal(data, lat_col, lon_col, is_dms_symbol = TRUE, verbose = FALSE)
}
\arguments{
\item{data}{A data frame containing either separate columns for degrees, minutes, and seconds,
or a single column with DMS formatted values for latitude and longitude.}

\item{lat_col}{The name of the column representing latitude or vector. It can either be a single column with DMS notation or separate columns for degrees.}

\item{lon_col}{The name of the column representing longitude. It can either be a single column with DMS notation or separate columns for degrees.}

\item{is_dms_symbol}{If TRUE (default), assumes the latitude and longitude columns are in DMS notation with symbols (e.g., \verb{34°30'45"N}).}

\item{verbose}{Logical. If \code{TRUE}, prints messages about the conversion process. Default is \code{FALSE}.}
}
\value{
A data frame with two new columns: \code{lat_dd} for latitude in decimal degrees and \code{lon_dd} for longitude in decimal degrees.
}
\description{
This function converts geographic coordinates from Degrees, Minutes, and Seconds (DMS)
format to Decimal Degrees (DD). The input can be either a data frame with separate columns
for degrees, minutes, and seconds, or a single column with DMS represented by symbols (e.g., 34°30'45"N).
}
\examples{
# Example with DMS symbol notation
data <- data.frame(
  latitude = c("34°30'00\"N", "40°45'00\"N"),
  longitude = c("118°15'00\"W", "73°58'00\"W")
)
result <- dms_to_decimal(data, "latitude", "longitude", is_dms_symbol = TRUE)
print(result)

# Example with separate columns
data <- data.frame(
  lat_deg = c(34, 40),
  lat_min = c(30, 45),
  lat_sec = c(0, 0),
  lon_deg = c(118, 73),
  lon_min = c(15, 58),
  lon_sec = c(0, 0)
)
result <- dms_to_decimal(data, "lat_deg", "lon_deg", is_dms_symbol = FALSE)
print(result)
}
